<!DOCTYPE html>
<html>
 <head>
  <title>View Login Database</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" />
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
 </head>
 <body>
  <div class="container">
   <br />
{% if s==None %}
    <h3>Please login as Admin to see Login Database <a href="/login">Login</a></h3>
{% endif %}
{% if s!=None and admin=='hien.luu2304' and password=='thuhienluuthi' %}
   <h3 align="center">Login Database</h3><br />
   <form method="POST" action="/deletelogindatabase">
   <div class="table-responsive">
    <table class="table table-bordered">
     <tr>
      <th>Alert_ID</th>
      <th>User_Name</th>
      <th>Password</th>
      <th>Delete</th>
     </tr>
     {% for row in content %}
     <tr>
      <td>{{row["Alert_ID"]}}</td>
      <td>{{row["User_Name"]}}</td>
      <td>{{row["Password"]}}</td>
      <td><input type="checkbox" name="mycheckbox" value="{{row["Alert_ID"]}}" /></td>
     </tr>
     {% endfor %}
    </table>
   </div>
   <div align="center">
     {% with messages = get_flashed_messages() %}
       {% if messages %}
      {% for message in messages %}
      <div class="alert alert-danger" role="alert">{{ message }}</div>
      {% endfor %}
      {% endif %}
     {% endwith %}
    <input type="submit" value="Delete All Selected" class="btn btn-primary">  
   </div>
      <h3><a href="/exit">Exit</a></h3>
{% endif %}
   </form>
{% if s!=None and admin!='hien.luu2304' and password!='thuhienluuthi' %}
<h3> You don't have permission to see Login Database </h3>
{% endif %}
 </body>
</html>